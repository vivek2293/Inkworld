package constants

// General routes
const (
	Version1 = "/v1"

	BookRoute = "/books"
)

// book routes
const (
	GetAllBookDetails  = "/all"
	GetBookDetailsByID = "/:id/details"
)

// other constants
const (
	IDParam = "id"
)
